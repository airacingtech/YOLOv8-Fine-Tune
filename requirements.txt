# Minimal requirements for the ART YOLOv8 fine-tuning pipeline.
# Use this as an alternative to environment.yml on a machine that already has
# CUDA 12.1 and PyTorch 2.5+ installed (e.g. a pre-configured training server).
#
# Install:
#   pip install -r requirements.txt

ultralytics>=8.3.0
torch>=2.5.0
torchvision>=0.20.0
opencv-python>=4.10.0
tqdm>=4.60.0
onnx>=1.17.0
onnxruntime-gpu>=1.19.0
